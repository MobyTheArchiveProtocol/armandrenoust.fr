---
const currentYear = new Date().getFullYear();
---

<footer class="site-footer">
    <div class="container">
        <p>© {currentYear} Armand Renoust - Artisan Web Pragmatique.</p>
        
        <div class="footer-links">
            <a href="https://linkedin.com" target="_blank">LinkedIn</a>
            <a href="https://github.com" target="_blank">GitHub</a>
        </div>
    </div>
    <div class="footer-legal">
            <a href="/mentions-legales">Mentions Légales</a>
        </div>
    </div>
</footer>

<style>
    .site-footer {
        /* CORRECTION : On enlève le gris, on met transparent */
        background-color: transparent;
        
        /* Une ligne de séparation à peine visible (5% d'opacité) */
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        
        padding: 2rem 0;
        margin-top: auto; /* S'assure que le footer est poussé tout en bas */
    }

    .container {
        max-width: var(--max-width);
        margin: 0 auto;
        padding: 0 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    p {
        margin: 0;
        font-size: 0.85rem;
        color: #666; /* Gris discret */
    }

    .footer-links {
        display: flex;
        gap: 1.5rem; /* Espace entre LinkedIn et GitHub */
    }

    .footer-links a {
        color: #888;
        font-size: 0.85rem;
        text-decoration: none;
        transition: color 0.2s;
    }

    .footer-links a:hover {
        color: #fff; /* Devient blanc au survol */
    }

    /* Mobile : On empile si l'écran est petit */
    @media (max-width: 600px) {
        .container {
            flex-direction: column;
            gap: 1rem;
            text-align: center;
        }
    }
    .footer-main {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .footer-legal {
        width: 100%;
        text-align: center; /* Centré ou à gauche selon préférence */
        margin-top: 2rem;
        padding-top: 1rem;
        border-top: 1px solid rgba(255,255,255,0.02); /* Ligne très très fine */
    }

    .footer-legal a {
        font-size: 0.75rem; /* Plus petit que le reste */
        color: #444; /* Très discret */
        text-decoration: none;
    }
    .footer-legal a:hover { color: #888; }
</style>